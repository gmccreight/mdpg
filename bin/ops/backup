#!/bin/bash

source `dirname $0`/_shared.sh

if [ $has_local_config ]; then

  date_str=`date +"%Y_%m_%d_%H_%M_%S"`
  rsync -av \
    --delete --backup --exclude="*BKUP_*" --suffix=BKUP_$date_str \
    $shared_mdpg_server:$shared_mdpg_remote_path/.app_data/ \
    $shared_mdpg_backup_dir

fi
